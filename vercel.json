{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/fetch-streak/:platform/:username",
      "destination": "/api/fetch-streak"
    }
  ]
}
